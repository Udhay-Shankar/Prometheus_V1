{
  "name": "prometheus-v1",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install:all": "npm install && cd server && npm install",
    "build": "npm run build",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
